curl -O https://bootstrap.pypa.io/get-pip.py     # install python
python3 get-pip.py --user
ls -a ~
export PATH=~/.local/bin:$PATH
source ~/.bash_profile
pip3 --version
pip3 install awscli --upgrade --user
aws --version
